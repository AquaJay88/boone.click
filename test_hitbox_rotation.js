// If the cylinder is added to the `model` but we didn't account for rotation?
// Actually wait! The model is scaled 1000, 1000, 1000.
// Is the model rotated?
// What about the hitBoxRadius?
// hitBoxRadius = pathRadius + 40 / 1000
// How big is pathRadius?
